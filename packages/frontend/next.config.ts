import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Empty turbopack config silences the "using webpack config with Turbopack" warning
  turbopack: {},
};

export default nextConfig;
